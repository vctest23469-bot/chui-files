# 参与贡献

欢迎改进 Chui Files 的本地文件管理体验、可靠性、无障碍支持与文档。

## 报告问题

请在 Issue 中提供应用版本、macOS 版本、Intel / Apple Silicon、复现步骤、预期结果与实际结果。涉及复制或移动时，说明源与目标是否跨卷、文件系统类型及是否包含符号链接。请使用脱敏样本，不上传私人文件、密钥或完整个人路径。

## 提交修改

1. Fork 仓库并创建分支，将变更限定为一个明确目标。
2. 文件操作逻辑的修改应提供临时目录内的回归测试，覆盖失败路径与不覆盖现有数据的约束。
3. 执行 `zsh scripts/test.sh` 和 `zsh scripts/build.sh`；UI 修改补充复现步骤与脱敏截图。
4. 在 Pull Request 中说明问题、修改后的行为、执行过的验证及未验证的平台。更新相应文档。

请勿提交 `build/` 下的应用、DMG、测试数据和个人配置。发布包由维护者通过 Release 分发。

## 开发约定

- 保持 SwiftUI + AppKit 原生实现，新增依赖需先说明必要性。
- 文档与界面默认使用中文，代码标识和 API 保留准确名称。
- 不将一次成功测试视为所有文件系统、权限和硬件环境均可用。
- 提交代码时请确认有权按本项目 MIT 许可证提供该贡献。
